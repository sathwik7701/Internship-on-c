Q 3- Write a C program to add Two One Dimensional Array elements and show the output.

The code is :

#include <stdio.h>

int main() 
{
    int size, i;
    int arr1[50], arr2[50], sum[50];

    printf("Enter the size of arrays: ");
    scanf("%d", &size);

    printf("Enter the elements of first array:\n");
    for (i = 0; i < size; i++) 
    {
        scanf("%d", &arr1[i]);
    }

    printf("Enter the elements of second array:\n");
    for (i = 0; i < size; i++)
    {
        scanf("%d", &arr2[i]);
    }

    printf("Sum of elements:\n");
    for (i = 0; i < size; i++)
    {
        sum[i] = arr1[i] + arr2[i];
        printf("%d ", sum[i]);
    }

    return 0;
}


OUTPUT IS :

Enter the size of arrays: 5
Enter the elements of first array:
1 2 3 4 5
Enter the elements of second array:
6 7 8 9 10
Sum of elements:
7 9 11 13 15

